-- import Lean

-- def hello := "world"

-- open Lean

-- namespace Py

-- inductive ShapeData :=
--   | wildcard
--   | var (x : Name)
--   | const (n : Nat)

-- inductive Shape :=
--   | nil
--   | wildcard
--   | var (x : Name)
--   | cons (data : ShapeData) (rest : Shape)
--   | append (op1 op2 : Shape)

-- end Py

-- instance {n : Nat} : OfNat Py.ShapeData n where
--   ofNat := .const n
def hello := "world"
